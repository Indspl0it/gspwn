---
title: DRM commands
description: "The 24 dispatched commands of /dev/dri, their command numbers, handler symbols and permission flags, the node each reaches, and the 4 the dispatch table omits."
---

Generated by `python3 tools/refgen.py` from `surface/drm-command-inventory.json`. `python3 tools/regression_check.py pages` regenerates the page and fails when the result differs from the committed copy.

`/dev/dri/cardN` and `/dev/dri/renderDN` reach a default `compute,utility` container on the CDI injection path, which is the current default. `GetDeviceNodesByBusID` at `nvidia-container-toolkit/internal/info/drm/drm_devices.go:27` globs the DRM directory of the GPU's PCI device and maps every entry into `/dev/dri`, with no capability check, and `internal/edits/device.go:76` grants them `rwm`. The legacy path never injects them: `libnvidia-container` carries no reference to `/dev/dri`. These commands are the seventh family of the surface denominator.

## Dispatch

nvidia-drm is a DRM driver, so the core owns the request-number encoding and every command carries its own number. `drm_ioctl` recovers the driver command with `_IOC_NR` minus `DRM_COMMAND_BASE`, so the command number is the ABI identity of a DRM target and the completion ledger is keyed on it. `_IOC_SIZE` bounds the copy and selects no handler.

| Quantity | Value | Source |
|---|---|---|
| Declared command numbers | 28 | `kernel-open/nvidia-drm/nv_drm_common_ioctl.h` |
| Populated table entries | 24 | `kernel-open/nvidia-drm/nvidia-drm-drv.c` |
| Entries with DRM_RENDER_ALLOW | 21 | `DRM_RENDER_ALLOW` |
| Entries with DRM_MASTER | 2 | `DRM_MASTER` |
| Entries with no permission flag | 1 | `0` |
| Command number base | `0x40` | `DRM_COMMAND_BASE` |
| ioctl type byte | `'d'` | `DRM_IOCTL_BASE` |
| Missing table entries | 4 | `nv_drm_ioctls[]` |

## Reachability by node

The two nodes do not grant the same set. `drm_ioctl_permit`, in the Linux DRM core at `drivers/gpu/drm/drm_ioctl.c`, refuses a render client any command whose flag word omits `DRM_RENDER_ALLOW`, and refuses any caller a `DRM_MASTER` command unless it is the current master. A tenant holds both nodes, so the family denominator is the union and counts 24. The per-node figures are carried apart because 24 and 21 are true of different things.

| Node | Reachable | Basis |
|---|---|---|
| `/dev/dri/cardN` | 24 | a primary client is subject to neither the render test nor, for 22 of them, the master test |
| `/dev/dri/renderDN` | 21 | the 21 commands carrying `DRM_RENDER_ALLOW` |

2 command(s) reach a handler on `cardN` only while the opening file is the current DRM master. `drm_master_open`, in the Linux DRM core at `drivers/gpu/drm/drm_auth.c`, makes the opening file the master when the device has none, which is likely on a host running no display server and is not guaranteed.

| Number | Command | Condition |
|---|---|---|
| `0x12` | `DRM_NVIDIA_GRANT_PERMISSIONS` | the opening file must be the current DRM master, which drm_master_open grants only when the device has none |
| `0x13` | `DRM_NVIDIA_REVOKE_PERMISSIONS` | the opening file must be the current DRM master, which drm_master_open grants only when the device has none |

## Commands

Ordered by command number, which is the offset from `DRM_COMMAND_BASE` that `drm_ioctl` indexes the driver table with.

| Number | Command | Handler | Direction | Parameter struct | Flags | Nodes |
|---|---|---|---|---|---|---|
| `0x00` | `DRM_NVIDIA_GET_CRTC_CRC32` | `nv_drm_get_crtc_crc32_ioctl` | `DRM_IOWR` | `drm_nvidia_get_crtc_crc32_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x01` | `DRM_NVIDIA_GEM_IMPORT_NVKMS_MEMORY` | `nv_drm_gem_import_nvkms_memory_ioctl` | `DRM_IOWR` | `drm_nvidia_gem_import_nvkms_memory_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x02` | `DRM_NVIDIA_GEM_IMPORT_USERSPACE_MEMORY` | `nv_drm_gem_import_userspace_memory_ioctl` | `DRM_IOWR` | `drm_nvidia_gem_import_userspace_memory_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x03` | `DRM_NVIDIA_GET_DEV_INFO` | `nv_drm_get_dev_info_ioctl` | `DRM_IOWR` | `drm_nvidia_get_dev_info_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x04` | `DRM_NVIDIA_FENCE_SUPPORTED` | `nv_drm_fence_supported_ioctl` | `DRM_IO` | none | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x05` | `DRM_NVIDIA_PRIME_FENCE_CONTEXT_CREATE` | `nv_drm_prime_fence_context_create_ioctl` | `DRM_IOWR` | `drm_nvidia_prime_fence_context_create_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x06` | `DRM_NVIDIA_GEM_PRIME_FENCE_ATTACH` | `nv_drm_gem_prime_fence_attach_ioctl` | `DRM_IOW` | `drm_nvidia_gem_prime_fence_attach_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x08` | `DRM_NVIDIA_GET_CLIENT_CAPABILITY` | `nv_drm_get_client_capability_ioctl` | `DRM_IOWR` | `drm_nvidia_get_client_capability_params` | `0` | cardN |
| `0x09` | `DRM_NVIDIA_GEM_EXPORT_NVKMS_MEMORY` | `nv_drm_gem_export_nvkms_memory_ioctl` | `DRM_IOWR` | `drm_nvidia_gem_export_nvkms_memory_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x0a` | `DRM_NVIDIA_GEM_MAP_OFFSET` | `nv_drm_gem_map_offset_ioctl` | `DRM_IOWR` | `drm_nvidia_gem_map_offset_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x0b` | `DRM_NVIDIA_GEM_ALLOC_NVKMS_MEMORY` | `nv_drm_gem_alloc_nvkms_memory_ioctl` | `DRM_IOWR` | `drm_nvidia_gem_alloc_nvkms_memory_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x0c` | `DRM_NVIDIA_GET_CRTC_CRC32_V2` | `nv_drm_get_crtc_crc32_v2_ioctl` | `DRM_IOWR` | `drm_nvidia_get_crtc_crc32_v2_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x0d` | `DRM_NVIDIA_GEM_EXPORT_DMABUF_MEMORY` | `nv_drm_gem_export_dmabuf_memory_ioctl` | `DRM_IOWR` | `drm_nvidia_gem_export_dmabuf_memory_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x0e` | `DRM_NVIDIA_GEM_IDENTIFY_OBJECT` | `nv_drm_gem_identify_object_ioctl` | `DRM_IOWR` | `drm_nvidia_gem_identify_object_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x0f` | `DRM_NVIDIA_DMABUF_SUPPORTED` | `nv_drm_dmabuf_supported_ioctl` | `DRM_IO` | none | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x10` | `DRM_NVIDIA_GET_DPY_ID_FOR_CONNECTOR_ID` | `nv_drm_get_dpy_id_for_connector_id_ioctl` | `DRM_IOWR` | `drm_nvidia_get_dpy_id_for_connector_id_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x11` | `DRM_NVIDIA_GET_CONNECTOR_ID_FOR_DPY_ID` | `nv_drm_get_connector_id_for_dpy_id_ioctl` | `DRM_IOWR` | `drm_nvidia_get_connector_id_for_dpy_id_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x12` | `DRM_NVIDIA_GRANT_PERMISSIONS` | `nv_drm_grant_permission_ioctl` | `DRM_IOWR` | `drm_nvidia_grant_permissions_params` | `DRM_UNLOCKED, DRM_MASTER` | cardN, while master |
| `0x13` | `DRM_NVIDIA_REVOKE_PERMISSIONS` | `nv_drm_revoke_permission_ioctl` | `DRM_IOWR` | `drm_nvidia_revoke_permissions_params` | `DRM_UNLOCKED, DRM_MASTER` | cardN, while master |
| `0x14` | `DRM_NVIDIA_SEMSURF_FENCE_CTX_CREATE` | `nv_drm_semsurf_fence_ctx_create_ioctl` | `DRM_IOWR` | `drm_nvidia_semsurf_fence_ctx_create_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x15` | `DRM_NVIDIA_SEMSURF_FENCE_CREATE` | `nv_drm_semsurf_fence_create_ioctl` | `DRM_IOWR` | `drm_nvidia_semsurf_fence_create_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x16` | `DRM_NVIDIA_SEMSURF_FENCE_WAIT` | `nv_drm_semsurf_fence_wait_ioctl` | `DRM_IOW` | `drm_nvidia_semsurf_fence_wait_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x17` | `DRM_NVIDIA_SEMSURF_FENCE_ATTACH` | `nv_drm_semsurf_fence_attach_ioctl` | `DRM_IOW` | `drm_nvidia_semsurf_fence_attach_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |
| `0x18` | `DRM_NVIDIA_GET_DRM_FILE_UNIQUE_ID` | `nv_drm_get_drm_file_unique_id_ioctl` | `DRM_IOWR` | `drm_nvidia_get_drm_file_unique_id_params` | `DRM_RENDER_ALLOW, DRM_UNLOCKED` | cardN, renderDN |

## Commands outside the denominator

4 of the 28 declared command numbers carry no entry in `nv_drm_ioctls[]`. Each has a request-number macro in the header and no handler behind it, and nothing else in the nvidia-drm tree refers to any of them, so none is a target and the family contributes 24 and not 28.

| Number | Command | Reason |
|---|---|---|
| `0x19` | `DRM_NVIDIA_REGISTER_ROI` | no entry in nv_drm_ioctls[] |
| `0x1a` | `DRM_NVIDIA_UNREGISTER_ROI` | no entry in nv_drm_ioctls[] |
| `0x1b` | `DRM_NVIDIA_GET_CRTC_ROI_CRCS` | no entry in nv_drm_ioctls[] |
| `0x1c` | `DRM_NVIDIA_GET_ROI_CAPABILITIES` | no entry in nv_drm_ioctls[] |

The declared range also has a hole in it. `0x07` carries no `DRM_NVIDIA_` define at all, so the declared count is not the highest number plus one.

## See also

- [Enumerated surface](/gspwn/reference/surface/)
- [Attack surface](/gspwn/architecture/attack-surface/)
