---
title: Enumerated surface
description: "The attack surface as browsable tables: escapes, control commands, allocation classes and the prior kernel-module CVEs, generated from the committed artefacts."
---

Generated by `python3 tools/refgen.py` from `surface/cve-hotspots.json`, `surface/ioctl-inventory.json`, `surface/prior-cves.json`, `surface/rm-chains.json`, `surface/rm-control-inventory.json`, `surface/rm-control-rank.json`, `surface/rm-object-graph.json`, `tools/ioctl_map.json`. `python3 tools/regression_check.py pages` regenerates the page and fails when the result differs from the committed copy.

Four pages render the enumerated surface of driver `610.57.04`. Every row is read from an artefact under `surface/`, and nothing on these pages is written by hand.

## Pages

| Page | Records | Contents | Source artefacts |
|---|---|---|---|
| [Allocation classes](/gspwn/reference/surface/allocation-classes/) | 253 | The allocatable classes, the allocation chains, and the cumulative reach curve | `surface/rm-object-graph.json`, `surface/rm-chains.json` |
| [Control commands](/gspwn/reference/surface/control-commands/) | 531 | The targetable control commands, ranked, with their owning class, depth and parameter size | `surface/rm-control-rank.json`, `surface/rm-control-inventory.json`, `surface/rm-object-graph.json` |
| [Driver CVEs](/gspwn/reference/surface/driver-cves/) | 61 | The disclosures NVIDIA places in the kernel modules this project fuzzes, and where reading the fixing diff placed each one | `surface/prior-cves.json`, `surface/cve-hotspots.json` |
| [Escapes](/gspwn/reference/surface/escapes/) | 37 | The dispatched RM escapes, the two multiplexers, and the declared escapes no switch reaches | `surface/ioctl-inventory.json`, `tools/ioctl_map.json` |

## Surface model totals

`tools/surface_cov.py` builds the denominator every later measurement joins against. A target holds one syzlang call name; an excluded population holds none, for the stated reason.

| Family | Targets | Meaning |
|---|---|---|
| `escape` | 32 | one `case` of the RM `switch` |
| `uvm` | 39 | one command of `/dev/nvidia-uvm` |
| `uvm_tools` | 7 | one command of `/dev/nvidia-uvm-tools` |
| `control` | 531 | one leaf of `NV_ESC_RM_CONTROL` |
| `alloc` | 155 | one leaf of `NV_ESC_RM_ALLOC` |

| Excluded family | Records | Reason |
|---|---|---|
| `control_gsp` | 236 | the CPU-side handler is compiled out under GSP offload |
| `uvm_test` | 104 | gated behind a build-time test switch |
| `escape_mux` | 2 | a dispatcher whose leaves are counted in the control and allocation families |
| `escape_dead` | 3 | declared in a header and dispatched by nothing |

Total targets: 764.

## Staleness

`python3 tools/regression_check.py pages` regenerates all five pages into a temporary directory and compares them against the committed copies, naming the page and the first differing line when they disagree. It runs in the same offline CI job as the other four artefact checks, so an artefact regenerated against a new driver release without regenerating these pages fails the build.

## See also

- [Attack surface](/gspwn/architecture/attack-surface/)
- [Artifacts](/gspwn/reference/artifacts/)
- [`surface_cov.py`](/gspwn/architecture/components/surface-cov/)
