---
title: Enumerated surface
description: "The attack surface as browsable tables: escapes, control commands, allocation classes and the prior kernel-module CVEs, generated from the committed artefacts."
---

Generated by `python3 tools/refgen.py` from `surface/cve-hotspots.json`, `surface/entry-points.json`, `surface/ioctl-inventory.json`, `surface/nvkms-command-inventory.json`, `surface/prior-cves.json`, `surface/rm-chains.json`, `surface/rm-control-inventory.json`, `surface/rm-control-rank.json`, `surface/rm-object-graph.json`, `tools/ioctl_map.json`. `python3 tools/regression_check.py pages` regenerates the page and fails when the result differs from the committed copy.

5 pages render the enumerated surface of driver `610.57.04`. Every row is read from an artefact under `surface/`, and nothing on these pages is written by hand.

## Pages

| Page | Records | Contents | Source artefacts |
|---|---|---|---|
| [Allocation classes](/gspwn/reference/surface/allocation-classes/) | 253 | The allocatable classes, the allocation chains, and the cumulative reach curve | `surface/rm-object-graph.json`, `surface/rm-chains.json` |
| [Control commands](/gspwn/reference/surface/control-commands/) | 531 | The targetable control commands, ranked, with their owning class, depth and parameter size | `surface/rm-control-rank.json`, `surface/rm-control-inventory.json`, `surface/rm-object-graph.json` |
| [Driver CVEs](/gspwn/reference/surface/driver-cves/) | 61 | The disclosures NVIDIA places in the kernel modules this project fuzzes, and where reading the fixing diff placed each one | `surface/prior-cves.json`, `surface/cve-hotspots.json` |
| [Escapes](/gspwn/reference/surface/escapes/) | 37 | The dispatched RM escapes, the two multiplexers, and the declared escapes no switch reaches | `surface/ioctl-inventory.json`, `tools/ioctl_map.json` |
| [Modeset commands](/gspwn/reference/surface/modeset-commands/) | 66 | The dispatched commands of /dev/nvidia-modeset, their dispatch ordinals and parameter structs | `surface/nvkms-command-inventory.json` |

## Surface model totals

`tools/surface_cov.py` builds the denominator every later measurement joins against. A target holds one syzlang call name; an excluded population holds none, for the stated reason.

| Family | Targets | Meaning |
|---|---|---|
| `escape` | 32 | one `case` of the RM `switch` |
| `uvm` | 39 | one command of `/dev/nvidia-uvm` |
| `uvm_tools` | 7 | one command of `/dev/nvidia-uvm-tools` |
| `control` | 531 | one leaf of `NV_ESC_RM_CONTROL` |
| `alloc` | 155 | one leaf of `NV_ESC_RM_ALLOC` |
| `modeset` | 64 | one leaf of the single `/dev/nvidia-modeset` request number |

| Excluded family | Records | Reason |
|---|---|---|
| `control_gsp` | 236 | the CPU-side handler is compiled out under GSP offload |
| `uvm_test` | 104 | gated behind a build-time test switch |
| `escape_mux` | 2 | a dispatcher whose leaves are counted in the control and allocation families |
| `escape_dead` | 3 | declared in a header and dispatched by nothing |
| `modeset_undispatched` | 2 | declared in `enum NvKmsIoctlCommand` with an empty dispatch entry |

Total targets: 828.

## Entry points

The command families above count `ioctl` targets. The driver also registers `mmap` and `poll` on the device nodes it serves, and those carry no method id, no parameter struct and no inventory row. They are counted here and never inside the 828 above.

| Device node | fops table | Entry points | Modelled |
|---|---|---|---|
| `/dev/nvidiactl`, `/dev/nvidiaN` | `nvidia_fops` | `poll`, `unlocked_ioctl`, `compat_ioctl` (under `NVCPU_IS_X86_64 or NVCPU_IS_AARCH64`), `mmap`, `open`, `release` | yes |
| `/dev/nvidia-uvm` | `uvm_fops` | `open`, `release`, `mmap`, `unlocked_ioctl`, `compat_ioctl` (under `NVCPU_IS_X86_64`) | yes |
| `/dev/nvidia-uvm-tools` | `uvm_tools_fops` | `open`, `release`, `unlocked_ioctl`, `compat_ioctl` (under `NVCPU_IS_X86_64`), `poll` | yes |
| `/dev/nvidia-nvswitchctl` | `ctl_fops` | `unlocked_ioctl` | lookup_devices at libnvidia-container/src/nvc_info.c:515 creates /dev/nvidiactl, /dev/nvidia-uvm, /dev/nvidia-uvm-tools and /dev/nvidia-modeset only, so a default container carries no node served by this table. |
| `/dev/nvidia-nvswitchN` | `device_fops` | `unlocked_ioctl`, `open`, `release`, `poll` | lookup_devices at libnvidia-container/src/nvc_info.c:515 creates /dev/nvidiactl, /dev/nvidia-uvm, /dev/nvidia-uvm-tools and /dev/nvidia-modeset only, so a default container carries no node served by this table. |
| `/dev/nvidia-caps/nvidia-capN` | `g_nv_cap_drv_fops` | `open`, `release` | libnvidia-container/src/nvc.c:218 creates a capability node only for a MIG-partitioned GPU, and only for the partitions the container was given. A default container on an unpartitioned GPU carries none. |
| `/dev/nvidia-caps-imex-channels/channelN` | `g_nv_caps_imex_fops` | `open`, `release` | lookup_devices at libnvidia-container/src/nvc_info.c:515 creates /dev/nvidiactl, /dev/nvidia-uvm, /dev/nvidia-uvm-tools and /dev/nvidia-modeset only, so a default container carries no node served by this table. |
| `/dev/dri/cardN`, `/dev/dri/renderDN` | `nv_drm_fops` | `open`, `release`, `unlocked_ioctl`, `compat_ioctl` (under `defined(CONFIG_COMPAT)`), `mmap`, `poll`, `read`, `llseek`, `fop_flags` (under `defined(FOP_UNSIGNED_OFFSET)`) | lookup_devices at libnvidia-container/src/nvc_info.c:515 creates /dev/nvidiactl, /dev/nvidia-uvm, /dev/nvidia-uvm-tools and /dev/nvidia-modeset only, so a default container carries no node served by this table. |
| `/dev/nvidia-modeset` | `nvkms_fops` | `poll`, `unlocked_ioctl`, `compat_ioctl` (under `NVCPU_IS_X86_64 or NVCPU_IS_AARCH64`), `mmap`, `open`, `release` | The node reaches a default tenant on the CDI path. controlDeviceNodeDiscoverer at nvidia-container-toolkit/pkg/nvcdi/common-nvml.go:52-63 lists it beside /dev/nvidiactl, /dev/nvidia-uvm and /dev/nvidia-uvm-tools with no capability check, and nvidia-container-toolkit/internal/info/auto.go:89 makes that path the default runtime mode. The legacy path needs the display capability: libnvidia-container/src/nvc_mount.c:786 skips the modeset minor unless OPT_DISPLAY is set, libnvidia-container/src/options.h:92 sets that flag from the display capability alone, and libnvidia-container/src/options.h:100 leaves display out of the default capability set. The description set opens a descriptor on the node and its 64 commands are inside the denominator. The mmap and poll entry points of this table are not modelled. |
| `/dev/nvidia-nvlink` | `nvlink_fops` | `open`, `release`, `unlocked_ioctl` | lookup_devices at libnvidia-container/src/nvc_info.c:515 creates /dev/nvidiactl, /dev/nvidia-uvm, /dev/nvidia-uvm-tools and /dev/nvidia-modeset only, so a default container carries no node served by this table. |

16 entry point(s) are registered on the 4 modelled device node(s), of 43 across every `file_operations` table the driver defines. The description set declares a call for each `mmap` and `poll` on a modelled node; `open` and `release` are reached by `openat` and by process exit, and `unlocked_ioctl` is the command families above.

## Staleness

`python3 tools/regression_check.py pages` regenerates all 6 pages into a temporary directory and compares them against the committed copies, naming the page and the first differing line when they disagree. It runs in the same offline CI job as the other six artefact checks, so an artefact regenerated against a new driver release without regenerating these pages fails the build.

## See also

- [Attack surface](/gspwn/architecture/attack-surface/)
- [Artifacts](/gspwn/reference/artifacts/)
- [`surface_cov.py`](/gspwn/architecture/components/surface-cov/)
